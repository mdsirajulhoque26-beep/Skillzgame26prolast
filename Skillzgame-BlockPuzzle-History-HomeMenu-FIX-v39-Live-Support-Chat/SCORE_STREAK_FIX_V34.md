# V34 — Matchmaking + Streak Score Fix

Targeted fixes only:

1. Open asynchronous SUBMITTED solo matches can now be joined by a later compatible player within the pending window.
2. Block Puzzle streak now resets to 0 immediately when a move clears no line, so streak bonus cannot continue increasing after a streak is broken.

Preserved: V32 submit fix, Admin match delete fix, Home loading fix, History, Settings, Multiplayer, and other game logic.
